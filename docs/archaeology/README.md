# Archaeology notes

These dated notes record subsystem reverse-engineering and evidence interpretation. Their technical content is retained as historical research rather than continuously rewritten documentation.

## Repository-path note

Many notes were written before the 2026-09-05 repository layout cleanup and therefore cite active TypeScript files under `web/...`. The active app was moved without content changes:

`web/...` **→** `rtao/...`

Treat those old paths as historical names; use the same relative path beneath `rtao/` when inspecting current source.
