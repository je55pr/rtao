# Read this first

This is the **post-reconstruction source-containing** Road Trip Adventure Three.js handoff.

The source root is no longer merely the Aug-30 baseline: the proven Aug-31 Q's Factory/night-renderer changes have been reapplied. Read `HANDOFF_NEXT_CHAT.md` for exact truths, remaining experiments, and validation status.

Key state:

- field cache v7 / `RTAFLD7!`;
- Q28/car-UV/QFactory layering fixes restored;
- 216k PAL clock + 1.1 VU coefficients restored;
- Original / Extended / Unlimited atmosphere restored;
- TFX/TCC retained and world-wide census reproved;
- exact GS MODULATE remains an experiment, not the normal renderer;
- full npm/Vite/Vitest validation is pending only because `node_modules` was lost by sandbox refresh.

`handoff/` preserves earlier recovery/snapshot notes for provenance.
`reference/RoadTripAdventureMonoGame-interiors.zip` is the bundled C#/PS2 archaeology reference.
